---
import Navigation from '../components/Navigation.astro';
import PageTransitionController from '../components/PageTransitionController.astro';
import PixelMediaLayer from '../components/PixelMediaLayer.astro';
import ThemeToggle from '../components/ThemeToggle.astro';
import Footer from '../components/Footer.astro';
import SiteSearch from '../components/SiteSearch.astro';
import LanguageToggle from '../components/LanguageToggle.astro';
import WebGLBackground from '../components/WebGLBackground.astro';
import siteData from '../data/site-data.json';
import type { Locale } from '../i18n';
import { localizedPath } from '../i18n';
import '../styles/global.css';
import '../styles/immersive.css';

interface Props {
  title: string;
  description?: string;
  locale: Locale;
  fullWidth?: boolean;
}

const { title, description, locale, fullWidth = false } = Astro.props;
const palette = siteData.palette || {};
---

<!DOCTYPE html>
<html lang={locale} data-theme={siteData.defaultTheme}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <meta name="theme-color" content={palette.primary?.hex || '#17131f'}>
    <link rel="icon" type="image/png" href="/logo.png" />
    <meta property="og:image" content="https://pub-dba2c40f6183450a9bfc05eeb62b7837.r2.dev/brand/OG-IMAGE-mirror-practitioner-context-reused-v1.png" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    {description && <meta property="og:description" content={description} />}
  </head>
  <body class={`immersive-body ${fullWidth ? 'immersive-body--fullwidth' : ''}`}>
    <WebGLBackground />
    <div class="immersive-shell">
      <header class="immersive-topbar glass-panel">
        <a href={localizedPath(locale, '/')} class="brand-lockup">
          <img src="/logo.png" alt="Klear Karma logo" width="32" height="32">
          <span class="brand-text">
            <strong>{siteData.brandName}</strong>
            <small>{siteData.tagline}</small>
          </span>
        </a>

        <button class="hamburger" id="menu-toggle">
          <span class="line"></span>
        </button>

        <nav class="immersive-topbar__links">
          <a href={localizedPath(locale, '/')}>Home</a>
          <a href={localizedPath(locale, siteData.docsHomeHref)}>Docs</a>
          <a href={localizedPath(locale, siteData.heroSecondaryHref || '/docs/research/notebooklm-artifacts')}">Artifacts</a>
        </nav>
      </header>

      {!fullWidth && (
        <aside class="immersive-sidebar" id="sidebar">
          <Navigation locale={locale} />
        </aside>
      )}

      <main class="immersive-content">
        <div class="content-wrapper">
          <SiteSearch locale={locale} />
          <slot />
        </div>
      </main>

      <Footer locale={locale} />
    </div>

    <ThemeToggle locale={locale} />

    <script>
      const menuToggle = document.getElementById('menu-toggle');
      const sidebar = document.getElementById('sidebar');
      const body = document.body;

      menuToggle?.addEventListener('click', () => {
        body.classList.toggle('sidebar-open');
      });

      // Close on link click
      sidebar?.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => document.body.classList.remove('sidebar-open'));
      });
    </script>

    <style>
      .immersive-body {
        min-height: 100vh;
      }
      
      .hamburger {
        display: none;
        appearance: none;
        background: none;
        border: none;
        padding: 0.5rem;
        cursor: pointer;
      }

      .hamburger span {
        display: block;
        width: 1.5rem;
        height: 2px;
        background: currentColor;
        margin-block: 4px;
      }

      @media (max-width: 768px) {
        .immersive-sidebar {
          position: fixed;
          left: -100%;
          width: 280px;
          transform: translateX(0);
          height: 100vh;
          transition: left 0.3s;
          z-index: 1000;
        }
        body.sidebar-open { overflow: hidden; }
        .sidebar-open .immersive-sidebar {
          left: 0;
        }
      }
    </style>
  </body>
</html>
